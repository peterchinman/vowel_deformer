#include <iostream>
#include <fstream>
#include <string>
#include <regex>


int main()
{
    // open CMU dict

    // read CMU dict line-by-line into a std::unorderd_map with word, pronunciation

    // define a vowel transformation

    // get user-text

    // read user-text word-by-word

    // check word against dict, get pronunciation

    // get vowels from pronunciation

    // apply vowel transformation

    // transform ARPABET -> pronouncable text
}
